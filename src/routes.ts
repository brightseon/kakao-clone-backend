// auth
const AUTH = '/auth';
const JOIN = `/join`;
const DUPLICATE_CHECK = `/duplicateCheck`;
const LOGIN = `/login`;

export const authRoute = {
    auth : AUTH,
    join : JOIN,
    duplicateCheck : DUPLICATE_CHECK,
    login : LOGIN
};